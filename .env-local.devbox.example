# Dreamkast devbox環境変数設定ファイル
#
# このファイルは .env-local.devbox にコピーされ、
# devbox run auth でAWS Secrets Managerから自動的に値が設定されます。
#
# 手動で設定する場合は以下のコメントを参考にしてください。
# 注意: .envファイルは export を使わない KEY=VALUE 形式で記述します。

# ===== 認証情報（自動取得推奨） =====
# devbox run auth で以下の値が自動設定されます
# - AUTH0_CLIENT_ID
# - AUTH0_CLIENT_SECRET
# - AUTH0_DOMAIN
# - RAILS_MASTER_KEY
# - AWS_ACCESS_KEY_ID
# - AWS_SECRET_ACCESS_KEY
# - AWS_SESSION_TOKEN

# Auth0設定（AWS Secrets Manager: dreamkast/reviewapp-env から自動取得）
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_DOMAIN=

# Rails Master Key（AWS Secrets Manager: dreamkast/rails-app-secret から自動取得）
RAILS_MASTER_KEY=

# ===== ローカル開発用設定（デフォルト値） =====

# Sentry（オプション、エラートラッキング）
SENTRY_DSN=

# AWS認証（ローカル開発ではLocalStackを使用するため必須ではない）
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# S3設定（LocalStackを使用するため開発時は不要）
S3_BUCKET=dreamkast-test-bucket
S3_REGION=ap-northeast-1

# MySQL設定（Docker Composeで起動するMySQLに接続）
MYSQL_HOST=127.0.0.1
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=dreamkast

# Redis設定（Docker Composeで起動するRedisに接続）
REDIS_URL=redis://127.0.0.1:6379

# SQS設定（LocalStackを使用）
SQS_FIFO_QUEUE_URL=http://localhost:4566/000000000000/default
SQS_MAIL_QUEUE_URL=http://localhost:4566/000000000000/default

# その他の設定
DREAMKAST_API_ADDR="http://localhost:8080"
AWS_REGION=ap-northeast-1
