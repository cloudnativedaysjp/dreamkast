name: Release new version based on .github/release.yml

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    uses: cloudnativedaysjp/reusable-workflows/.github/workflows/rw-release-new-version.yml@main
