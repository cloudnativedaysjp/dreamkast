<%= form_with(url: admin_speaker_path, model: @speaker) do |form| %>
  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      Name: <%= @speaker.name %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      Profile: <%= @speaker.profile %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      Company: <%= @speaker.company %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      job_title: <%= @speaker.job_title %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      twitter_id: <%= @speaker.twitter_id %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      github_id: <%= @speaker.github_id %>
    </div>
  </div>

  <div class="tw-mb-4">
    <% if speaker.avatar_url.nil? %>
      <div class="avatar">Avatar: <%= image_tag 'dummy.png', :size => '80x80'  %></div>
    <% else %>
      <div class="avatar">Avatar: <%= image_tag speaker.avatar_url(:small) || avatar_url, :size => '80x80'  %></div>
    <% end %>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      <%= form.label "Avatarを変更: " %>
      <%= form.file_field :avatar %>
    </div>
  </div>

  <div class="tw-mb-4 tw-grid tw-grid-cols-12 tw-gap-4">
    <div class="tw-col-span-12 md:tw-col-span-6">
      <%= form.submit %>
    </div>
  </div>
<% end %>
