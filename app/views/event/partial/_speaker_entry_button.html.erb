<% if (conference.registered? && conference.speaker_entry_enabled?) || (conference.opened? && @speaker.present?) || (conference.closed? && @speaker.present?) %>
  <p class="text-white font-weight-bold">登壇者募集中!</p>
  <%= link_to speaker_entry_button_name, speakers_entry_path, {method: :get, class: "btn btn-secondary btn-xl  m-2" } %>
<% end %>
