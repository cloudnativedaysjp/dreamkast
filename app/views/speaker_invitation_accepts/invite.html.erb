<% provide(:title, 'プロポーザル募集') %>

<div class="container">
  <div class="row speaker-dashboard-form p-md-4" id="speaker-landing">

    <div class="col-12 my-4">
      <h2 class="text-center">共同スピーカーとして招待された方へ</h2>
      <p>本ページは共同スピーカーとしての招待メールを受け取った方向けのページです。</p>
      <p>下記の概要・Q&Aを確認していただき、問題なければ「エントリーする！」ボタンをクリックしてください。</p>
      <p>その後、登壇者情報の入力フォームに遷移しますので、入力することで共同スピーカーとして登録されます。</p>
    </div>

    <%= render 'speaker_dashboard/speakers/guidance_section_1' %>

    <div class="col-12 align-self-baseline my-4 justify-content-center text-center">
      <% if logged_in? %>
        <%= button_to 'エントリーする！', new_speaker_invitation_accept_path, {params: {token: @speaker_invitation.token}, method: :get, class: "btn btn-secondary btn-xl inline" } %>
      <% else %>
        <%= button_to 'エントリーする！', '/auth/auth0', {method: :post, class: "btn btn-secondary btn-xl inline" } %>
      <% end %>
      <% if @conference.abbr == 'cnk' %>
        <%= label_tag 'entry', 'エントリーにはクラウドネイティブ会議へのサインアップが必要です' %>
      <% else %>
        <%= label_tag 'entry', 'エントリーにはCloudNative Daysへのサインアップが必要です' %>
      <% end %>
    </div>

    <%= render 'speaker_dashboard/speakers/guidance_section_2' %>

    <div class="col-12 align-self-baseline my-4 justify-content-center text-center">
      <% if logged_in? %>
        <%= button_to 'エントリーする！', new_speaker_invitation_accept_path, {params: {token: @speaker_invitation.token}, method: :get, class: "btn btn-secondary btn-xl inline" } %>
      <% else %>
        <%= button_to 'エントリーする！', '/auth/auth0', {method: :post, class: "btn btn-secondary btn-xl inline" } %>
      <% end %>
      <% if @conference.abbr == 'cnk' %>
        <%= label_tag 'entry', 'エントリーにはクラウドネイティブ会議へのサインアップが必要です' %>
      <% else %>
        <%= label_tag 'entry', 'エントリーにはCloudNative Daysへのサインアップが必要です' %>
      <% end %>
    </div>
  </div>
</div>
