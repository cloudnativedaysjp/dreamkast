こんにちは、<%= @speaker.name %> さん。
<%= @conference.name %> のプロポーザルを以下の内容で受け付けました。

タイトル: <%= @talk.title %>
概要: <%= @talk.abstract %>
<% @talk.selected_proposal_items.each do |k, v| %>
<%= k %>: <%= v %>
<% end %>

提出したプロポーザルの確認・修正は登壇者ダッシュボードから行えます。

<%= link_to '登壇者ダッシュボード', speaker_dashboard_url(event: @conference.abbr) %>

<%= render '/layouts/footer' %>
