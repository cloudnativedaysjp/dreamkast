<%= render "layouts/modal", title: "招待" do %>
  <% flash.each do |key, value| %>
    <div class="dk-alert dk-alert-danger" role="alert"><%= flash[:alert] %></div>
  <% end %>
  <h1 class="entry tw-mb-4 tw-text-center tw-text-xl tw-font-semibold">スポンサー担当者に招待メールを送信する</h1>
  <%= render "sponsor_dashboards/sponsor_contact_invites/form",
      sponsor: @sponsor,
      sponsor_contact_invite: @sponsor_contact_invite
  %>
<% end %>
