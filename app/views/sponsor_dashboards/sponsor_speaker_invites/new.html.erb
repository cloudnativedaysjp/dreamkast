<%= render "layouts/modal", title: "招待" do %>
  <div class="row justify-content-center">
    <div class="col-12 col-lg-12 py-3 px-md-5">
      <h1 class="text-center py-3 entry">スポンサー登壇者に招待メールを送信する</h1>
      <%= turbo_frame_tag "sponsor_speaker_invite_form" do %>
        <%= render "form", sponsor: @sponsor, sponsor_speaker_invite: @sponsor_speaker_invite %>
      <% end %>
    </div>
  </div>
<% end %>
